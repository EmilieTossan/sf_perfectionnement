{% extends "admin/base.html.twig" %}

{% block title %}Auteur{% endblock %}

{% block main %}

    {% for writer in writers %}

        <h2><a href="{{ path("admin_writer_show", {'id' : writer.id})}}">{{ writer.name }}</a></h2>

    {% endfor %}

{% endblock %}