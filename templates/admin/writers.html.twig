{% extends "admin/base.html.twig" %}

{% block title %}Auteur{% endblock %}

{% block main %}

    {% for writer in writers %}

        <h2><a href="{{ path("admin_show_writer", {'id' : writer.id})}}">{{ writer.firstname }} {{ writer.name }}</a></h2>

    {% endfor %}

{% endblock %}