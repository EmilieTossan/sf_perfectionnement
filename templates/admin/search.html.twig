{% extends "admin/base.html.twig" %}

    {% block main %}
    
    <h1>Les résultats de la recherche pour "{{ term }}" sont :</h1>

    {% for writer in writers %}
        <h2>{{ writer.firstname }} {{ writer.name }}</h2>
    {% endfor %}

    {% endblock %}
