{% extends "admin/base.html.twig" %}

{% block title %}Ajouter un auteur{% endblock %}

{% block main %}

    {{form_start(writerForm)}}

        {{form_label(writerForm.name, 'Nom de l\'auteur')}}
        {{form_widget(writerForm.name)}}

        {{form_label(writerForm.firstname, 'Prénom de l\'auteur')}}
        {{form_widget(writerForm.firstname)}}

        {{form_widget(writerForm.Enregistrer)}}

    {{form_end(writerForm)}}

{% endblock %}