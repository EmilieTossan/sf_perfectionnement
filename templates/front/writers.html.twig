{% extends "front/base.html.twig" %}

{% block title %}Auteur{% endblock %}

{% block main %}

    {% for writer in writers %}

        <h2><a href="{{ path("writer_show", {'id' : writer.id})}}">{{ writer.name }}</a></h2>

    {% endfor %}

{% endblock %}